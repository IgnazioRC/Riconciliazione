# _lab

Script diagnostici e one-shot non parte del flusso principale.

- **verifica_usd.py** — verifica le patch USD leggendo il sorgente di ms_reconciler
  con match di stringa. Usato per validare v2.1.1. Ora che l'USD è validato,
  spostato qui per evitare che un refactor renda falsi i suoi controlli testuali.
